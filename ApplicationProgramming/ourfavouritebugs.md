As programmers, we got into a ground we know:
- AI development, compared to classical programming in it's own means, shows that additional experience is needed for classical programmers.

The environment, right now, has this degree of breakage:
- Copy-paste
  - Markdown, markdown as text, markdown as formatted text;
    - copy-paste is very funny.
  - Code from PDF or code from HTML;
    - you lose even indents, which is especially funny when you copy python code.
  - Copy from CoPilot, ChatGPT etc.
    - has so many issues that it's funny.

You can see a general rule:
- Copy-paste is classical function, and it used to be so well-analyzed that every single path was closed.
- AI programming is specific area of programming, and it looks ad-hoc as long as classical programmers do not join.

We need this environment:
- We analyze it all: copy-pase from, what and where, and other copy paste issues:
  - Makes it look like a simple thing, which just happens.

For example, if you copy your own text from chat, mostly you lose newlines. If you paste markdown to wysiwyg forums, even from html interface with formatting, formatting is lost.

This is a typical low-level OS function, which should have drivers, use cases, compability stories etc.:
- In this sense, telling we won't need programming skills because of an AI is either very futuristic, or more probably a naïve and childish mythology about gods helping us:
  - For real, this is a serious skill of classical programmers, who need to update it to current types of software, such as inference chat, which is 90% broken in classic programming sense;
    - AI programmers quite intentionally give you rather libraries and command line tools which are *fully functional*,
      - but in the agile, imperative world they would be surprices if you just want to use Ctrl-C: it should be a scope of classical programmers to analyze the 100 use cases of Ctrl-C and all
        the different stories people can get our of it;
        - and it's the user role future to think: ctrl-c is trivial command to copy information you see.

I would give you long list of issues:
- With box where you write questions.
- Formats you get answers in, such as "\`\`\`" starts the article block is only way to copy-pase, but inside the next "\`\`\`" just breaks it.
- AI's consciousness of it's environments.

This is:
- Standard programming
- Rather than inventing anything original, you can be market boss by working on simple thing like textbox, or tweaking a standard WYSIWYG editor
- Altough AI communication seems easy, just enter the text and get answer, just in one format:
  - The intelligent machine runs this easy scenario to such myriad of cases that you *do have the grand scenario*;
    - this needs classical intelligence to analyze this text input box, and it's funny how deep you can get with input of a text line even in console:
      - but it's not funny any more how far it can reach with an AI.
