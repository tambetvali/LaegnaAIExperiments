# Object Oriented Chatting

Here, I will introduce set of trivia classes for object orientation in chat Q&A, which we will use later. These are not only AI generated code files, because the description would take more or less the same space - I like to use AI for extensive library use, but this one is rather a first-class cityzen python language use.